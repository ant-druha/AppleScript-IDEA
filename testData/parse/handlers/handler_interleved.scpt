prop MyPropNam: "Hello"

get MyPropNam
get MyPropNam

local LocalVar_New,ddd_dsad

get LocalVar_New
get ddd_dsad


on instancesRespondToSelector:str
end instancesRespondToSelector:

current application's NSWindow's instancesRespondToSelector:"endSheet:"

on areaOfRectangleWithWidth:www height:h
  return www * h
end areaOfRectangleWithWidth:height:

--different ways of calling this handler type:
tell someScriptObject to areaOfRectangleWithWidth:10 height:5--MultiRangeReference
someScriptObject's areaOfRectangleWithWidth:10 height:5
my areaOfCircleWithRadius:10

tell it to areaOfRectangleWithWidth:10 height:5
tell it to arsasaaOfRecsSSSSStangleWithWidth:10 heSSSSight:5

areaOfRectangleWithWidth

on areaOfCircleWithRadius:rad
  return rad ^ 2 * pi
end areaOfCircleWithRadius:rad:

on areaOfCircleWithCoord:{xCoor,yCoor}
  return xCoor ^ 2 * pi
end areaOfCircleWithRadius_2:radius:

my areaOfCircleWithCoord:{5,5}

on areaOfCircleWithPatterenParams:{name:myNamE, attributes:myAttr}
  get myAttr
  return myNamE ^ 2 * pi
end areaOfCircleWithRadius_3:radius:#todo correctly resolve this

on factorial3({name:myName, attributes:myAttr})
  get myAttr

  get myName
  get x
end

factorial3(2323)

on factorial2(x)
  get x
  factorial2(x-1)--todo fix resolve
  if x > 0 then
    return x * factorial2(x - 1)
  else
    return 1
    set myLocal to "ddsds"
  end if
end factorial2


factorial2(3)

set t to 9
copy "C" to c
copy 8 to r
set a to r

it's tableView:t  objectValueForTableColumn:c row:r --should not be couted for usages and resolve
tableView_objectValueForTableColumn_row(t, c, r)

on tableView_objectValueForTableColumn_row(t, c, r)
  get c
end tableView_objectValueForTableColumn_row_

tableView_objectValueForTableColumn_row(t, c, r)

on tableView:t objectValueForTableColumn:c row:r
  get t
  get r
end tableView:objectValueForTableColumn:row:


on tableView_6:t
  get t
  get r
end tableView_6:

tell him to tableView_6:3
tableView

ddd_dsad

tell it to tableView:"dsd"

tell it to tableView
it's tableView:5objectValueForTableColumn:4row:5

on foo:f bar:r bazzz:z

end foo:bar:baz:

tell it to foo:3 bar:9 bazzz:2

tell it to foo:3 bar:9 bazzz:a

it's tableView:4objectValueForTableColumn:3row:5
